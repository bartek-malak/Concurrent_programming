<Window x:Class="PresentationView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PresentationViewModel;assembly=TP.ConcurrentProgramming.PresentationViewModel"
        Title="Symulacja Kul">

	<Window.DataContext>
		<vm:MainWindowViewModel />
	</Window.DataContext>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<Border Grid.Row="0" BorderThickness="4" BorderBrush="Red" Margin="10" Background="White" Width="378" Height="318">
			<ItemsControl ItemsSource="{Binding Balls}">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<Canvas />
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>
				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<Ellipse Width="{Binding Diameter}" Height="{Binding Diameter}" Fill="Blue" />
					</DataTemplate>
				</ItemsControl.ItemTemplate>
				<ItemsControl.ItemContainerStyle>
					<Style TargetType="ContentPresenter">
						<Setter Property="Canvas.Top" Value="{Binding Top}" />
						<Setter Property="Canvas.Left" Value="{Binding Left}" />
					</Style>
				</ItemsControl.ItemContainerStyle>
			</ItemsControl>
		</Border>

		<StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
			<Label Content="Ilość kul:" VerticalAlignment="Center"/>
			<TextBox Width="50" Margin="5" TextAlignment="Center"
                     VerticalContentAlignment="Center"
                     Text="{Binding BallCount, UpdateSourceTrigger=PropertyChanged}"/>

			<Button Content="START" Command="{Binding StartCommand}"
                    Padding="20,5" Margin="10,0"/>
		</StackPanel>
	</Grid>
</Window>